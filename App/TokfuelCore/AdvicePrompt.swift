import Foundation

/// ヒントは「何が起きているか」しか言わない。読んで自分で対策を組み立てる手間を省くため、
/// 描画から切り離した純粋関数にしてあるので、文面はヘッドレスで検査できる
/// （`AdvicePromptTests`）。ここが出すのは文字列だけで、送信も保存もしない。
public enum AdvicePrompt {
    public static func text(for advice: RetokReport.Advice, source: String) -> String {
        """
        Claude Code などの利用コストを可視化する Tokfuel が、私の直近の使い方から\
        次の指摘を出しました。

        # 指摘（出どころ: \(source)）
        \(advice.title)

        # ツールが挙げた根拠
        \(advice.detail)

        # 依頼
        この指摘を、いま私が作業しているリポジトリでの実際の進め方に当てはめてください。\
        そのうえで次の 3 つを出してください。

        1. 何が起きているのかの解釈。推測が要るなら、置いた前提を明示する
        2. 今日から変えられる具体的な手順。設定値・コマンド・運用のどれなのかまで書く
        3. 変えた効果をどう確かめるか

        指摘の前提が私の状況に当てはまらないと思ったら、無理に対策を出さずにそう言ってください。\
        判断に足りない情報があれば、埋める前に何が要るかを聞いてください。
        """
    }
}
