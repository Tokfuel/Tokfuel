# TestDocs

アプリ（`App/Tokfuel`）向けのテストシナリオ設計を置く場所です。

テンプレート、観点 ID の規約、個別シナリオの起票は後続 Issue で足します。
このディレクトリは配置の箱だけを先に確保しています。

実行される UT / IT は [`../Tests`](../Tests/) に、通しテスト実装は [`../E2E`](../E2E/) に置きます。
