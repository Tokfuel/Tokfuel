---
id: Cost-00-example-slug
title: （短いシナリオ名）
primary_domain: Cost
platforms: [macOS]
status: ideation
# issue: <URL または #NNNN>（関連 Issue があるときだけ。無いときはこの行ごと省略）
# superseded_by: Cost-00-example-slug（archived にするとき、後任があれば）
---

## シナリオ

（ユーザー視点の操作と観測結果を、です・ます調で書きます。複数ある場合は番号付きにします。）

（観測する範囲と、観測しない範囲の境界を一文で書いても構いません。）

（実装クラス名やファイルパスはここに書きません。見た目の固定は完了条件の VRT で扱います。）

## 完了条件

完了条件は手段ごとのステップ箇条書きです。進捗や実装手段の詳細は書かない（`status` / `## 対応済みPR` へ）。  
通しの操作と観測は **E2E** を主とします。計算や表示用モデルの組み立てなど、通しだけでは脆い観測は **UT&IT** を補助に足します。見た目の固定が要るときだけ **VRT** を足します。

- **E2E**
  - （例: メニューバーからホームを開き、モデル別の行が見える）
- **UT&IT**
  - （例: 期間切替後の集計範囲が、選んだ期間と一致する）
- **VRT**
  - （例: 推移グラフの切替後の見え方が、プレビュー用フィクスチャとして固定されている）

経路ごとの再現手順は次の「## 経路」に書きます。完了条件を経路の下には置きません。  
備考は真の制約（例: 異常系は別観点）があるときだけ書き、進捗・未着手・実装手段は書かない。

## 経路

**経路**は、実装するテスト（スイート）単位です。1 観点は複数経路（複数テスト）になり得ます。  
見出しは振る舞いのみにします（クラス名は書かない。Bad: `### PopoverViewTests` / Good: `### ホームを開いて主要セクションが見える`）。その下に GWT を書きます。  
節名に「前提条件」は使いません（前提は Given に書きます）。

**Then** は Store / Settings / 表示用の状態を主に expect します。ネットワーク呼び出し回数などは補助観測とし、主検証にはしません。

### ホームを開いて主要セクションが見える

- **Given**：（例: フィクスチャ付きの利用データがある）
- **When**：（例: メニューバーからホームを開いた時）
- **Then**：（例: 今日の見出しと主要セクションが見える）

## 対応済みPR

PR 作成後に追記する。作成前は空、または `（未作成）`。  
コミットハッシュの羅列やファイルパスのダンプは書かない（PR を見れば足りる）。任意で一行のスコープのみ。

- （未作成）

記入形式:

```text
- [#NNNN](https://github.com/Tokfuel/Tokfuel/pull/NNNN) （短い説明）
```
